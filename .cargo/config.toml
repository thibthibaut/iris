[alias]
scan = "run -- scan"
metadata = "run -- metadata"
geo = "run -- geo"
quality = "run -- quality"
embed = "run -- embed"
faces = "run -- faces"
cluster-faces = "run -- cluster-faces"
ocr = "run -- ocr"
webp-cache = "run -- webp-cache"
show-db = "run -- show-db"
serve = "run -- serve"
index = "run -- index"
